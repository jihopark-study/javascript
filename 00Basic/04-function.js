/* -------------------------------------------------------------------------- */
/*                                 기명함수(함수선언식)                         */
/* -------------------------------------------------------------------------- */
function check() {
  alert("기명함수 입니다.");
}
/* -------------------------------------------------------------------------- */
/*                               매개변수 Parameters                            */
/* -------------------------------------------------------------------------- */
//<button type="submit" onclick="sendName('홍길동')">
// 인수 = 아규먼트 : 데이터

// function sendName(isName) {
// 인자 = 파라미터 = 매개변수 : 변수

function sendName(isName) {
  alert(`${isName}님의 데이터가 전송되었습니다.`);
}
/* -------------------------------------------------------------------------- */
/*                                   다중 매개변수                              */
/* -------------------------------------------------------------------------- */
function add(num1, num2) {
  alert(`${num1}과 + ${num2}의 합은 ${num1 + num2}입니다.`);
}
/* -------------------------------------------------------------------------- */
/*                                  매개변수 기본값                             */
/* -------------------------------------------------------------------------- */
function defaultName(name = "지호") {
  alert(`${name}님 안녕하세요.`);
}
/* -------------------------------------------------------------------------- */
/*                                 익명함수(함수표현식)                         */
/* -------------------------------------------------------------------------- */
// 기명함수와 익명함수는 큰 차이는 없다. 표현 방식의 차이.
const multiply = function (number1, number2) {
  alert(number1 * number2);
};

/* -------------------------------------------------------------------------- */
/*                                 화살표함수                                    */
/* -------------------------------------------------------------------------- */
const moveX = (step) => {
  const character = document.querySelector(".character");
  //   character.style.translate = `${step}px`; or
  character.style.translate = step;
};
